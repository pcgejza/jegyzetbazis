
<span class="leftHolder">
    <a href="{{path('frontend_index_homepage')}}"><span class="headerLogo"></span></a>
        {% include 'FrontendLayoutBundle:Header:menu.html.twig' %}
</span>
<span class="searchHolder">
    {% include 'FrontendSearchBundle:Default:search_header_box.html.twig' %}
</span>
<span class="rightHolder">
    {% if User is not defined or User is null %}
        <button class="login">Bejelentkezés</button>
        <button class="registration">Regisztráció</button>
    {% else %}
        {% include 'FrontendLayoutBundle:Header:headerUser.html.twig' %}
    {% endif %}
</span>